import { createTheme } from "@mui/material/styles";

const WHITE_COLOR = "#fff";
const PRIMARY_COLOR = "#4723d9";

export const theme = createTheme({
  palette: {
    primary: {
      main: PRIMARY_COLOR,
    },
  },
});
